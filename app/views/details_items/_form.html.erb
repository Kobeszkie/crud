<%= form_with(model: details_item) do |form| %>
  <% if details_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(details_item.errors.count, "error") %> prohibited this details_item from being saved:</h2>

      <ul>
        <% details_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :price, style: "display: block" %>
    <%= form.number_field :price %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
